import React from 'react';
import TableContainer from "../containers/TableContainer";

const HomePage = () => {
    return (
        <div>
            <TableContainer />
        </div>
    );
};

export default HomePage;
